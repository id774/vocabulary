.span10.offset1
  - if @accounts.length > 0
    %table{:class => 'table table-bordered table-striped'}
      %thead
        %tr
          %th 似たような語彙のユーザーアカウント
      %tbody
        %tr
          %td
            - @accounts.each do |account|
              = link_to account, "http://twitter.com/#{account}", :target => "_blank"

  - unless @values.nil?
    %table{:class => 'table table-bordered table-striped'}
      %thead
        %tr
          %th 語彙(件数)
      %tbody
        %tr
          %td
            - @values.each do |k, v|
              = k + "(" + v.to_s + ") "

  %p{:class=>"pull-right pager"}
    = link_to "語彙抽出器に戻る", root_path
